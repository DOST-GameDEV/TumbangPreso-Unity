# Rejected Rafi model, 2026-09-22

The owner explicitly rejected the current model as unlike the rest of the cast.
The agent built a separate box recipe with Geometry.bevel and a native donor rig;
using the native skeleton alone did not make the geometry match the approved cast.
These files preserve that rejected attempt, not an approved baseline.

Replacement instruction: locate the existing old voxel builder in this repository,
make a dedicated copy and edit that recipe for Rafi, comparing with its approved
outputs. Preserve the original builder and existing characters. Keep selected B
identity and kit/map/source progress; do not restart unrelated work or validations.
