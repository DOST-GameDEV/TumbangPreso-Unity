# D2 preparation and room directory

The previous login is preserved exactly. The owner explicitly excluded it from
redesign while this batch was in progress. The uncommitted gate/form proposal was
withdrawn; none of its builders or images are active. This is saved in AGENTS and
the UI plans. Two unused art explorations remain labelled as such in ArtSource.

Preparation now centres the real16:9 map preview, with native seat portraits and
neutral bot/open-seat marks along its lower edge. A clipped match-plan sheet
separates mode, bot difficulty, custom rules and spectating. Host/guest, room code,
profile, loadout, ready/start, chat and ranked callbacks retain their existing
owners and data. The room directory has a dedicated code entrance and separate
nearby/online list; it no longer uses account field/action sprites or wallpaper.

## Focused evidence

rooms-v3: five cases passed, profile/input restorationcd49fbd565fe. These cover
Classic/Hero preparation map/mode/custom-rules/loadout/Back; ranked state;
an actual local friends host with hidden chat history and room controls; directory
source lists/empty validation/Back; and cancelled delayed joins followed by a
newer attempt. This is not a real remote Relay join or a new two-player match test.

browser-v4: one focused recheck passed, restoration0a7f11ce2e7f. Visual inspection
after the five-case pass caught the second line of JOIN A / ROOM missing, although
preferredHeight fitted. Increased its allocated height and added an actual generated
line/character assertion. Revised screenshot visibly contains both lines.

Classic, Hero, ranked and the closed friends preparation view, plus nearby/online
directory empty states, cover960x540,1280x720,1366x768,1920x1080,1920x1200,
1280x960,2560x1440,3440x1440,3840x1080 and3840x2160. Visible action bounds,
text fit and14pixel reading floor are checked. Captures use isolated Unity render
targets, not physical monitor certification. The existing hidden chat stays active
at CanvasGroup alpha0 to retain history; the visible-layout qualifier now ignores
only fully invisible inherited alpha, not offscreen/clipped visible controls.
Expanded chat, populated long-name directory rows and active queue/fill states
still require their own visual checks during the remaining UI qualification.

Source originals and login are unchanged; no player build this batch. Existing
controller18callout diagram remains protected. No Figma, paid API, reset or agents.
Next settings, character/equipment and other UI compositions, U8 native ordinary-speed
motion/routes, then the entire GAMEPLAY_RESUME_AFTER_UI queue. No owner acceptance
or full UI completion claimed by these technical passes.
