# Cloud changes: local review and repair, 2026-09-24

Owner priority: review and fix the newly pushed cloud work before returning to the
older TODO path. The owner first asked us to wait, then explicitly confirmed the
push. Pulled ASTRAReworks 4f62fcc5c by fast-forward from 7fcde1877; no local work was
discarded. PR5 is already merged. Preserve all older tasks and published fixes.

Read cloud research/plan/progress and its lane ledger. These are source/pose-sheet
claims, not native Unity acceptance. The new native baseline includes:

- Seven authored ultimate introductions, individual duration/shot/lift/stage data,
  moving holds, shared cohort timing and protocol52.
- Revised body casts and 21 individual first-person gesture paths, Grand Coven's
  live cast/circle, and the final cloud commit's upright-effect material correction.
- Skill-tree testing unlock switch, plus voice routing and the team's script.
  The latest owner decision is HUMAN RECORDINGS ONLY. Generated clips/generators
  were removed in135a7595c; missing recordings must remain silent, without captions
  or blocking the announcer. Do not revive earlier synthetic-voice work.
- Condensed CLAUDE instructions with the full original preserved.

## Order

1. Native compilation and the existing seven-hero introduction study with its
   actual stages enabled, plus SharedUltimatePhaseTests. Question: do the cloud
   changes load/render and preserve accepted cohort, release, reduced-view and
   fallback behavior? Stop after the XML and actual frames, then fix concrete
   defects. No new player build at this stage. One fixture repair at most.
2. Inspect each hero's native body/stage/camera sequence individually against its
   plan. Preserve successful art. For observed problems, change one performance
   at a time and use paired fixed-camera views and25percent greyscale; normal-speed
   playback must be distinguished from still inspection.
3. Inspect live Grand Coven and the revised body/FPP casts. Then the skill-tree
   screen/unlocked and enforced progression paths and missing-recording behavior.
   Use existing probes; do not turn this into a new capture framework.
4. Address remaining cloud-owned skill VFX/SFX work from its per-skill plan only
   after incoming regressions/visual problems are handled. The other20world skills
   were explicitly not completed by cloud; do not silently check them off.
5. Reconcile all new evidence and remaining network/audio/device gates in TODO and
   ledger. Return to the older queue after this owner-prioritized work.

## Safety and evidence

Use the existing isolated QUAL checkout/warm cache, guarded Unity and named profile,
Direct3D11, one heavy job, frozen .cs inputs during every Unity run. Preserve
protected DEV PNG metas and back up generated QUAL churn before restoring it.
No Desktop replacement, paid service, synthetic voice generation, cross-chat work
or broad unchanged test reruns. Current source is authoritative over stale notes.
Final exact-player/real-peer protocol52 checks remain required; compile/stills alone
cannot close the cloud handoff or the overall goal.

## First native finding

Baseline7cases:2passed/5failed,32.0029462s. Each failure is Unity's key-index
assertion in GroundIntroduction's first root-position SetCurve, still present
after135a7595c. Its x/y/z replacement uses matching final times but briefly leaves
the old sparse y/z curves while setting dense x. Remove the whole old root-position
binding with the documented null-curve API before adding the synchronized curves;
preserve all authored rotation, lift and support sampling. Test that causal change
with the same focused set, then inspect actual frames. No fixture change/repair.
[Unity SetCurve API](https://docs.unity.com/en-us/engine/6000.0/script-reference/unityengine/animationclip/setcurve).

Clearing the old position binding did NOT resolve the native assertion;v2 is
preserved as failed evidence. The assertion remains on the first dense SetCurve
after SampleAnimation. Next causal change builds a fresh unsampled legacy clip
with the sampled grounding curves present from creation, reusing ClipBuilder's
original rotation/punch curves. The sampled temporary is disposed. No authored
timing/poses/lift are changed. Check the existing seven-hero study first, without
another whole shared-phase run until the grounding fault is resolved.

Separate source concern to assess after grounding: Receive derives cohort duration
from local actor kits before PrepareActors has proved those kits ready. A late
scene/roster reconstruction could pick the2.8default for a4.2second host phase.
This is not yet a reproduced peer failure; inspect message ordering and preserve
the shared deadline when deciding a fix, rather than widening this grounding run.
