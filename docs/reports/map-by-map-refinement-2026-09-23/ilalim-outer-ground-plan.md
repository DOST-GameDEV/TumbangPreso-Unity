# Ilalim outer ground edge,2026-09-24

The district view reveals a hard visual ground edge at120m. Native inventory/source
confirms FarGroundPlate is a240m-square visual slab without colliders, top-.06m.
Expand only its rendered x/z extent to3000m, retaining y/height/material and every
playable collider. Reapply existing surface coordinates only to that slab so the
asphalt/ground grain keeps its metre scale. No new district or gameplay area.

Use the existing district view at the same pose to check the exposed edge, then
capture the final preview/card, near shops and sky/Low view. Keep useful existing
shop fixtures and supplied signs unless those views reveal a concrete defect.
This does not complete native performance or integrated camera traversal.
