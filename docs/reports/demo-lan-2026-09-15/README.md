# Two-process direct connection baseline

The preserved nativev2 build passed one direct loopback host/client scenario in
Hero Strike. Both processes reported protocol37, Eskinita, active round2, defender
slot1 and the same structural hash0589B31D. The existing matrix evaluator found no
hard faults. This crosses a real round boundary; it is not two idle menu instances.

The client writes its report and exits before the host. The host's later report
shows its seat handed to a bot. Sampling occurs at105 and118seconds respectively,
so score and travel totals are not identical or simultaneous pose comparisons.
Do not present this as a latency measurement or exact moment-by-moment agreement.

Both processes used isolated demo-lan-v1-host/client profiles. Batch mode bypassed
external UGS sign-in, while explicit host/join kept host/client topology. Controls
were driven by the existing all-bots diagnostic. Preservation:0 pre-existing files
needed restoration and shared standalone input stayed unchanged. Owned processes
exited and were collected. The wrapper checks its exact fresh output path before
calling the older matrix runner and preserves prior result folders.

Artifact: Builds/demo-2026-09-16-v2/TumbangPreso.exe, Runtime SHA256
D161737889CC2048C24BCD8BFDFF02694875C99E2BDD4BC9DD083B6B90C0F3F7.
This baseline predates the later Nemu palm correction. Results/raw peer reports
and preservation receipt are included.

Remaining: two physical PCs/firewall/venue Wi-Fi, human responsiveness, real LAN
rematch/rejoin and the larger interruption/latency/variant matrices. The offline
native demo path is already separately qualified. The full queue stays active.
