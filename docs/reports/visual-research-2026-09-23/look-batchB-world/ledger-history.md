# Active TUMP rework ledger

## Current resume: batchB1.8/1.9/1.10, 2026-09-23

Owner is asleep. Continue every autonomously actionable queued item in THIS
conversation, one agent, without questions or checkpoint endings. Save the exact
state here at each meaningful checkpoint. Do not mark the full goal complete.

DEV: C:/Users/matth/Documents/GitHub/TumbangPreso-Unity-ASTRAReworks, ASTRAReworks.
Current HEAD/verified remotebbab70bbd30953039791a272c3bf9884d1a8f396 published1.7.
Earlier33d50bb7=1.5,88f9e6e68=1.2,479be6a53=1.1,f018173c4=UX-1intake.
ACTIVE background shell43053, batchB-v7: explicit guarded profile serialization
then guarded map/final-lighting tests. No.cs edits until the whole shell completes.
Selected final shadow level.44 aims2..2.5 after v6 measured2.7978 at.37.
Earlier v5stroke,v4maps and v2can checks retained. BatchB LOCAL/incomplete.
No native build. Original2PNGmetas protected; known4churn restored between invocations.


Completed1.1: exact ConfinementRadius square at sampled ground heights, rest/armed/
restore sweep; per-camera nearest exit with nested-camera restoration; physical
escape dust/swish excluding teleports/rounds; quiet local air; recorded shared box
and escape playback, personal cues excluded. WorldCueProfile owns off/intensity.
Imported edges are separate read-only meshes named default using Mat_chalk;
material plus whole thin bounds identifies4edges. No mesh/collider mutation.
Third/final look retained: quieter near chalk, stronger far armed chalk, two small
side wisps. DisableBatching fixes the billboard's origin. Do not tune it again.

Evidence: reports/visual-research-2026-09-23/look-1.1-world/report.md.
v8 geometry/all5maps passed1/1 (38.242s); v11 ownership/nested camera/real crossing/
teleport and visual study passed2/2 (15.297s); v3 recorded state and audible-to-muted
replay passed in its2/3 run. Failed case there was the subsequently fixed settled-
movement fixture. Normal and25percent-grey images personally inspected. Controlled
30fps puff study is not free-play or human acceptance. Native/full gate stays P7.
All earlier failures remain in Logs and ledger-history.md beside this report.

Completed1.2: camera-only Defense upper rim plus four open floor brackets for
actual taggable targets; nested views restore original art/identity. Bounded
distance lift for world cast/lata, not environments/viewmodels. Shared close clock
faces viewers, fills blue from actual restore and drains gold from protection.
Existing SyncLata optionally appends version1 and two presentation floats (9bytes),
retaining the old prefix/host guard/protocol50/authority. Existing clip11 State
packs8bit progress with presence bit1024; flags choose restore/protection, values
remain<=2048. No invented clock for legacy clips. Replay reconstructs its own clock.

Evidence: look-1.2-world/report.md beside the1.1 report. v3 wire/legacy/NaN/nonhost/
stale checks passed in3/3. v4 target/nested-camera/catchability passed in2/2. v7
strengthened actual-held restore/cancel/protection/encoding/capture passed1/1,
11.383s. Full and25percent-grey target/clock frames personally inspected. Retain
target look2 and clock look2. No more subjective tuning or passed checks. v5/v6
failures preserved; previous paused photography cancelled the channel, corrected
by freezing Carrier only during photographs, then resuming real input. Cancel now
waits its actual Update/LateUpdate handoff. No weakened assertions. Final static
review also cleans destroyed registry keys and refreshes can renderers on skin
change; next compilation/P7 includes those small lifecycle refinements.

Completed1.5: current source already had no LATA DOWN world popup; new actual
event test confirms no extra ComicPopup. V2 meaning/shape/colour/place comments
on owners. Existing ColourGrade now has optional peripheral travel strokes,
already-played footstep bearing pips, and accepted-ultimate world desaturation
with depth-tested colour retention for visible Toon cast/props/render copies.
All3WorldCueProfile experiment controls default0 pending owner's later choice.
No new HUD framework or edits to completed HUD/tint priority/recede.

Evidence: look-1.5-experiments/report.md. v2 all3cases passed19.393s (actual
sprint/sound guards, real reserved ultimate, body colour and occluding wall).
v3 corrected FPP personal views passed2/2,13.211s. Personally inspected full normal
frames and25percent-grey comparisons plus pip detail crop. Keep first usable look:
speed quiet/peripheral, sound a small hollow8-sector diamond, world grade preserves
cast/can colour. Owner may prefer them off; default decision is external review.
v1 own shader reserved-word error and wrong global speed assumption preserved;
v2 personal shots unhid own head and were INVALID, replaced by v3. Stop iterating.

Completed1.7: camera-plane parent compensates world75..110 to fixed95 apparent
viewmodel FOV for all child surfaces/effects. Scoped pooled snapshots restore
pose, lens scale, blocks, shadows and private visibility in both nested camera
orders; spectator POV uses the same class. Rest scale.64 versus.72 and seat.08m
lower; arms keep lit Toon, held shoe gets separate.16rim, private shadows off.
No imported model/material/palette/rig/builder changes. One.12s wrist set lives
inside charge. Existing.055contact/.18follow/.54settle now carries a lower-screen
pivot sweep past centre; offsets removed before each pose, zero at settle, extra
motion disabled by reduced motion/off. Carrier aim/launch/timing unchanged.

Evidence look-1.7-viewmodel/report.md. v1 passed2/2,14.780s: invariant apparent
held bounds75/95/110, real world lens, physical origin, both camera orders, off,
lit/rim/shadow, accepted charge/immediate release and protected refusal. v5 passed
new strengthened release witness1/1,9.771s: real projectile firstviewport
(.5005,.4999), hand crosses lowercentre to x.4368, stays visible, settles to0.
Personally inspected full before/after rest, charge, FOV/comfort/grey25, and
post-LateUpdate34-frame60Hz release studies v4(before) andv5(after). Keep rest
look1/release look2. GIF timestamps rounded to centiseconds matching60Hz average.
v2CSV namespace compile fault retained; v3pre-LateUpdate photos invalid for pose
timing, corrected by test-only LateUpdate20000 pump. Do not repeat these checks.

NEXT: run the next scoped batchB-v4 check, inspect its pictures, and retain
passed risks. BatchB is not ready to publish. The current batch scope:
1.8 lighting,1.9 court,1.10 hero objects, followed byP3,C,D,UX-1,P6,P7. Save a
concrete batchB plan before edits. Required1.8 design is NATIONALS_POLISH lines214+:
2..2.5:1 lit/shadow via lower tinted trilight, per-map ramp/tint, upper away-light
rim, label-safe metal highlight,10..15percent foot gradient, ground-contact
darkening/blob shadows, horizon fog and stable bands. Keep Built-in and two bands.
Read-only finding: Editor/MapKit/MapAtmosphereAuthor.Apply currently uses sky.65,
equator~.46,ground.34 and fogstart68/85,end165/190. Runtime MapGrade is data only;
ColourGrade owns camera adjustment. Do not darken the whole grade to fake shadows
or rebuild maps blindly. Preserve each map's architecture and source materials.
1.9 contrast medium per map, chalk wear/home circle/floor variation;1.10 can
findability, real airborne landing mark, short seat-coloured slipper flight ink.
Existing private/shared cues must continue to compose with these world changes.

Carry1.2 image finding into1.11/1.12: restoring has a large red grab crescent;
review its meaning/dominance then. Real peer/native/human acceptance remains P7.
No task deletion, completed HUD redo or login/mainmenu edits.

Original dirty home-court.png.meta/loading-street.png.meta under Resources/UI/
composition-redesign MUST NEVER be committed. Expected hashes in
Logs/visual1-world-protected-metas.json; original bytes in
Logs/look-1.1-v1/protected-inputs/. Known generated churn (two Inday FppDetails
assets,QualitySettings.asset,TimeManager.asset) restored after1.7v5; protected2
metas verified/restored exactly. Stage by path, never blanket add/reset/clean.
Fetch before push, verify remote HEAD. Sole author M4tyu633, no trailers/AI/em dash.

Unity ONLY through python tools/run_unity_guarded.py, profile
presentation-validation-20260921, never --help. No.cs edits during a run. Before
each run record one question and stopping result here. Smallest changed-behavior
set only; once passed return to implementation. Own defects fixed; unrelated or
environmental failures get at most one retry then exact blocker and independent
work. No intermediate builds/full regression: P7 only, internal Builds/ path.

UX-1 brief and11sketches saved in reports/front-end-flow-2026-09-23 and ArtSource.
Owner correction overrides attached brief: DO NOT TOUCH LOGIN OR MAIN MENU.
New Home opens only from existing TAP TO START. Queued P5.5 before P6/P7. Existing
HUD pass excluded. Required documents were read in order; do not restart intake.

Open carried issues:127.3 v57 chat-line clip recheck and native shutdown0xc0000005
in D3D12Core.dll1.618.1.0 offset0x264831, after all15review stages passed. P7 owns
the native investigation. No clean-exit claim. Preserve original characters,
builders, rigs,GUIDs,UGS IDs and saves. No paid services/resets/delegation/main/
Desktop replacement. Prior ledger is archived with the1.1,1.2,1.5 and1.7 reports; it is history,
not the current resume point.

Final static1.7 scope review: ChargeAnticipation now also requires _carrying, preserving the existing empty-hand lunge charge. Held-charge proofs retained; next compilation/P7 includes this one-condition exclusion. No extra rerun needed for unchanged active branch.

## BatchB concrete source-grounded plan before edits

Keep Built-in, existing2-band Toon, imported geometry/material assets, map layouts,
colliders, gameplay rules and completed HUD. Work all three world requirements into
one coherent batch; no native build. Separate authorable0off controls reproduce
the starting look. Same-camera before/after at5maps, normal/comfort/grey25, plus
small behavior checks for state/ownership/ground sampling. Max3subjective looks.

1.8 lighting: create an authorable Resources WorldLookProfile with5map entries
(ambient sky/equator/ground, shadow ramp/tint, horizon fog, court medium/wear kind).
Use a small generated linear1D ramp per map in Toon.shader. Keep2bands; stabilize
transition with BandEdge. Runtime map owner caches original lighting and blends
only on installation/weight changes so it does not fight SkyEvent. MapAtmosphereAuthor
should share the same target values for future authored maps, without rebuilding
existing maps. No whole-frame grade darkening. Target lit/shadow2..2.5:1, confirm
from pictures/calibration rather than constants alone. Shader globals scoped to
actual world cameras (main/recorded/ultimate); previews retain their own look.

Toon body: modest upper/away-key-light rim, subdued when rule/power rim already
owns attention; feet10..15percent darker fading out near chest, from actual actor
height. Lata highlight confined to metal-facing end caps/thin rim geometry so
owner labels keep their colours. Distance lift already exists1.2; compose with it,
do not add neon or bigger meshes. Static map style applies in replay; existing
RecordedEnvironment already records actual ambient/fog/sun values.

Environment contact: WorldOutline is ALREADY a screen-space depth/normal pass with
a depth-tested exclusion mask for Toon bodies, not per-mesh hulls. Reuse that pass
for a subtle lower-wall contact darkening within.5m of the local court floor,
excluding cast/props and horizontal road; do not add hundreds of mesh passes or
replace imported world shaders. Preserve its existing optional ink semantics.
Add small soft contact blobs under actual players/can/loose shoes using filtered
ground sampling and ground distance; no colliders/area-safety shape. Recorded
views reconstruct from recorded poses, live blobs hidden with VfxRenderTag.

1.9 court: material/whole-renderer discovery, no read of imported non-readable
meshes. Preserve four rule edges and all line locations. Chalk on asphalt/dark
deck; charcoal/brick on light paving/tile. Apply map medium to both live boundary
and remaining authored home/throw marks; original materials restored at off.
Static broken edges/smudges, scuffed home circle, subdued broad floor patches,
negative space in the active centre. Distinct wear by medium (paver dust, asphalt
oil/bleach, bridge damp concrete, roof tile dust, lagoon salt/board wear). Sample
actual court heights; overlays only, no water-covering slab or collision change.

1.10 props: evaluate real can readability together with lighting/court, remove
remaining permanent red-rim emphasis when the new object treatment is on. A small
dotted ground cue follows actual falling/airborne geometry. FACT: Lata.ApplyTilt
keeps the normal knock's lowest point on ground via radius*sin(tilt), so there is
no normal ballistic launch to predict. Show the real brief toppling/settle contact
and support elevated authoritative poses, do not invent gameplay flight. Replay
can derive the cue from its existing flags/recorded rotation and position.
SlipperMotionAccent already emits normal trails in both modes and records them,
but uses OwnerSlot and Sprites/Default. Change the ordinary stroke to actual
ThrowerSlot identity plus a short two-tone ink shader; keep affinity effects owned
by their existing systems. RecordedFlightStroke uses the same ordinary ink path.

Smallest checks after full batch: all5map baseline/off/on geometry and lighting
captures; actual catchable/restore cues still present (no old suite repeat); actual
knock/settle and elevated snapshot landing marker; real thrower-versus-owner trail
and recorded ordinary stroke; ground support/blob lifecycle and replay independence.
Record one question/stopping condition before launch. Own defects fixed, unrelated
failures at most one retry. Do not spend the whole turn on material/tool repair.

## BatchB current source checkpoint (uncompiled, no active job)

Added WorldLookProfile(5map data entries) and WorldLookPresentation(cache/restore
ambient/fog, scoped world-camera shader ramp/globals, no grade darkening). Wired
WorldCueProfile.WorldLighting/CourtSurface/HeroObjects off levers, profile author,
MatchInstaller, per-body/can properties and recorded-view equivalents. Toon now
samples the per-map ramp, adds upper away-key rim, foot gradient and end-cap-only
metal accent. Lata legacy red down-rim fades out with HeroObjects. MapAtmosphereAuthor
shares target ambient/fog for future explicit authoring; existing scenes not rebuilt.

WorldOutline now reuses its existing depth/normal/exclusion pass for lower-wall
contact shading, preserving the user's ink toggle separately; low graphics skips
the contact addition. Added WorldGround filtered support queries. Added
CourtSurfacePresentation/CourtSurface.shader: only low, nearby authored chalk
renderers get reversible material swaps; no source mesh read/change.28x28court
overlay samples support, omits missing/too-low cells, uses5distinct wear media and
subtle home scuff/centre lift. Existing dynamic boundary adopts the same map medium.
All of this is UNVERIFIED. No Unity launched since1.7; all.cs edits are safe now.

NEXT implement remaining contact blobs + actual falling/elevated can marker with
recorded counterparts, then SlipperMotionAccent/RecordedFlightStroke ordinary ink
and actual ThrowerSlot colour. Then write the smallest batchB fixture and ONE first
compile/qualification run with its explicit question/stop. Do not test half a batch.
Review recorder camera context before claim: RecordedEnvironment already carries
ambient/fog, but new static world shader/map/contact context must be applied to
recorded/ultimate cameras without changing preview art or gameplay protocol.

BatchB first complete source batch now LOCAL/UNVERIFIED: WorldLookProfile/Presentation+camera context; shared2-band ramp/body foot+upper rim/can cap accents; WorldOutline lower-wall contact reuse; reversible CourtSurface material/wear overlays; WorldGround/GroundContactVisual/WorldContactPresentation live+recorded/ultimate contact support and brief real settling/elevated can footprint; ordinary InkFlight shader with actual ThrowerSlot identity and matching replay stroke. No scene/model/collider asset rebuild. WorldCueProfile controls restore baseline at0. Added3focused Stage cases:5maps before/after/comfort and lighting/off/camera scope; actual can topple/settle/elevated authoritative pose + independent recorded ground support; real different-owner throw and recorded trail. Before-run question: does the whole batch compile, preserve geometry/authority, restore off state, retain replay/private camera boundaries and produce readable five-map world comparisons? Stop after3fresh cases pass and personally inspected normal/grey25 frames, max3looks; no native/full suite. C# freeze during run. Outputs Logs/look-batchB-v1/world.xml,world.log,shots. Remaining ratio/visual judgment is not claimed from shader constants.

BatchB-v1 compiled,0/3passed,40.383s,guard8acaa0afd9e9,job77408 reaped;4churn/2metas restored. Viewed Bayan before/after,Eskinita after and can-settling full frames. Own faults: settled marker uses imported renderer bounds as flight state; now reads a presentation-only getter of actual grounded tilt offset (no physics change). Map discovery reached Bayan/Eskinita then0marks on next map; new bounded failure message reports real chalk bounds/tags/scene before guessing. Flight case now waits presentation LateUpdate and reports actual state/position/velocity/active flags if still missing. First visual look not accepted yet: full-court surface patch appears too polygonal and apparent cast contrast needs actual qualification. Do not tune blindly while source faults remain. v2 repeats SAME3cases/question for these bounded fixes/diagnostics, no added suite/native build, no.cs edits while running.

Resume note during v2: recorded can support still uses renderer bottom and must
receive the same physical-support correction as live once this run ends. Do not
edit.cs in flight. Need measured map-discovery/flight diagnostics before more
changes, and isolate the polygonal floor patch from contact shading if it persists.
No batchB row is complete. Source/visual failures are retained, not external blockers.

BatchB-v2:1/3passed,33.234s,guard98c7bab33ab2,job11167 reaped;4churn/2metas restored. REAL can settle/elevated support/record independence case PASSED after physical support-offset fix; retain it. Map failure diagnostic: Ilalim has no material-name chalk candidates except the new boundary; CourtSurface now also honors explicit Chalk parent/node names, matching established boundary discovery. Source proves HostThrow correctly refuses another player's owned shoe; fixture now uses the actual supported unowned spare and real HostForceEquip, retaining different-owner/thrower assertion. Recorded can support now uses the same mesh-radius/tilt law rather than render-bottom guess. Added only2Eskinita diagnostic isolation pictures (lighting-only and surface-only) to identify the polygonal patch without blind tuning. NEXT: run only the still-failing map/stroke cases in batchB-v3; inspect diagnostics, then repair/tune measured issues. No Unity job currently active, C# safe; no native build. BatchB still incomplete, not published.

Before batchB-v3: question is whether explicit authored mark names restore remaining-map discovery and a legitimately unowned spare produces actual thrower-coloured ink, and which isolated layer causes the visible court patch. Run ONLY the map and ordinary-stroke cases. Retain v2can-support pass. Stop at those2fresh passes plus inspected normal/grey/isolation images; no native/full suite, no.cs edits during run.

BatchB-v3:1/2passed,42.257s,guard114835434eb3,job94091 reaped;4churn/2metas restored. Five-map geometry/off/scoped shader test PASSED, including Ilalim after explicit Chalk naming. Viewed isolation pictures: polygonal patch comes from CourtSurface, NOT contact darkening. Fixed wear overlay to multiplicative lit-floor modulation (preserves actual shadows) and removed abrupt dark-stain/white-lift branch. This is look2. Source reading corrected fixture again: ownerless stock is allowed ONLY in explicit training; test now enters the actual guided-training identity gate and restores its flag in teardown. No runtime ownership change. Can-support pass fromv2 retained. Next: run map look2 plus corrected stroke case; then inspect all images/grey25 and qualify actual lighting response before claiming batchB done. Current source dirty, no job active, C# edits safe. Record fresh run question/stop before launch.

Before batchB-v4: verify multiplicative floor-wear look2 keeps actual shadows and that explicit guided-training stock exercises real thrower identity and replay ink. Run ONLY5map capture/off contract and corrected stroke case; retain v2can support pass. Stop after2fresh passes and inspected normal/grey25, then qualify lighting response. No.cs edits in flight, no native/full suite.

BatchB-v4 ended1/2passed,42.074s,guardb7bf5ef17d0d,job52398 reaped;4churn/2metas restored. Five-map/off/camera-scope and new multiplicative look2 capture PASSED. Personally inspected full Bayan/Eskinita after: polygonal patch is gone, original shadows preserved. Still need all5normal/grey review and actual toon-light response qualification. Guided-training trail now launches/emits with correct shader/thrower. Failure is exact color expectation vs TrailRenderer8bit storage (.934706 requested vs .933333 stored); expected value now uses Color32 encoding, no tolerance widening. NEXT run ONLY corrected stroke case plus a targeted lighting calibration if needed; no map-contract repeat unless a look/source change warrants it. BatchB remains incomplete/unpublished. No active job, C# edits safe.

Before batchB-v5: run ONLY OrdinaryFlightInkUsesActualThrowerAndReplaysItsRecordedStroke. Question: does the real guided-training spare retain the correctly8bit-encoded actual thrower colour through live ink, recording, replay and off/re-equip cleanup? Stop on pass; all map and can checks retained. No.cs edits during run, no native/full suite. Outputs Logs/look-batchB-v5/world.xml,world.log,shots.

BatchB-v5 PASSED stroke1/1,6.812s,guard84b80b438847,job24998 reaped;4churn/2metas restored. All existing batch behavior cases now passed acrossv2/v4/v5. Viewed all5map after frames and15framegrey25 sheet fromv4: multiplicative wear removes the visible polygon, fog gives depth, court medium improves home mark on light plaza; cast lighting contrast is not obvious enough to claim the requested response. Added ONE targeted raw-HDR lighting measurement using a temporary uniform Toon cube (not a map asset), actual key light, world camera marker, top vs away-facing side samples, pre-render global weight and body shader/flash/probe metadata. Question for v6: does the real ramp/ambient change raise linear lit-to-shade contrast toward2..2.5, and is it active in the actual render? Stop after measured result; no map/stroke/can rerun or native build. Output Logs/look-batchB-v6/lighting-response.csv via shots/ and XML/log. No.cs edits in flight.

BatchB-v6 lighting measurement PASSED its exploratory bounds,7.037s,guardcd26b3f3b109,job8181 reaped;4churn/2metas restored. Actual pre-render weights0/1 confirmed. Raw HDR top/side ratio increased1.9472 to2.7978; body shader Toon,flash0,BlendProbes. This is stronger than the preferred2..2.5 target, so final look3 changes only ShadowLevel.37 to.44. Explicit editor authoring command persists that one new-profile scalar through Unity serialization; it never auto-overwrites later owner choices. Calibration assertion is strengthened to2..2.5. Before v7: serialize selected value, then run ONLY map comparisons and raw lighting case for final look3. Keep can/stroke passes. Stop after calibration within target and inspected final5map normal/grey25; no more subjective variants or native build. Two sequential guarded Unity invocations in one background shell, no.cs edits until the shell completes. Logs/look-batchB-v7/author.log and world.xml/world.log/shots.
