# Active TUMP rework ledger

## Current resume: VISUAL-1.7, 2026-09-23

Owner is asleep. Continue every autonomously actionable queued item in THIS
conversation, one agent, without questions or checkpoint endings. Save the exact
state here at each meaningful checkpoint. Do not mark the full goal complete.

DEV: C:/Users/matth/Documents/GitHub/TumbangPreso-Unity-ASTRAReworks, ASTRAReworks.
Current HEAD/verified remote33d50bb7993891d31d72b0023bf6132de2654760 published1.5;
88f9e6e68 published1.2,479be6a53 published1.1,f018173c4 UX-1 intake. ACTIVE guarded Unity job98593,look-1.7-v5 (release appearance only). No.cs edits
until completion.1.7 source/tests LOCAL; v1 passed2/2,14.780s,guardc57adf5ed442.
First rest/charge look inspected normal/grey25; keep it. No native/server/browser.

Completed1.1: exact ConfinementRadius square at sampled ground heights, rest/armed/
restore sweep; per-camera nearest exit with nested-camera restoration; physical
escape dust/swish excluding teleports/rounds; quiet local air; recorded shared box
and escape playback, personal cues excluded. WorldCueProfile owns off/intensity.
Imported edges are separate read-only meshes named default using Mat_chalk;
material plus whole thin bounds identifies4edges. No mesh/collider mutation.
Third/final look retained: quieter near chalk, stronger far armed chalk, two small
side wisps. DisableBatching fixes the billboard's origin. Do not tune it again.

Evidence: reports/visual-research-2026-09-23/look-1.1-world/report.md.
v8 geometry/all5maps passed1/1 (38.242s); v11 ownership/nested camera/real crossing/
teleport and visual study passed2/2 (15.297s); v3 recorded state and audible-to-muted
replay passed in its2/3 run. Failed case there was the subsequently fixed settled-
movement fixture. Normal and25percent-grey images personally inspected. Controlled
30fps puff study is not free-play or human acceptance. Native/full gate stays P7.
All earlier failures remain in Logs and ledger-history.md beside this report.

Completed1.2: camera-only Defense upper rim plus four open floor brackets for
actual taggable targets; nested views restore original art/identity. Bounded
distance lift for world cast/lata, not environments/viewmodels. Shared close clock
faces viewers, fills blue from actual restore and drains gold from protection.
Existing SyncLata optionally appends version1 and two presentation floats (9bytes),
retaining the old prefix/host guard/protocol50/authority. Existing clip11 State
packs8bit progress with presence bit1024; flags choose restore/protection, values
remain<=2048. No invented clock for legacy clips. Replay reconstructs its own clock.

Evidence: look-1.2-world/report.md beside the1.1 report. v3 wire/legacy/NaN/nonhost/
stale checks passed in3/3. v4 target/nested-camera/catchability passed in2/2. v7
strengthened actual-held restore/cancel/protection/encoding/capture passed1/1,
11.383s. Full and25percent-grey target/clock frames personally inspected. Retain
target look2 and clock look2. No more subjective tuning or passed checks. v5/v6
failures preserved; previous paused photography cancelled the channel, corrected
by freezing Carrier only during photographs, then resuming real input. Cancel now
waits its actual Update/LateUpdate handoff. No weakened assertions. Final static
review also cleans destroyed registry keys and refreshes can renderers on skin
change; next compilation/P7 includes those small lifecycle refinements.

Completed1.5: current source already had no LATA DOWN world popup; new actual
event test confirms no extra ComicPopup. V2 meaning/shape/colour/place comments
on owners. Existing ColourGrade now has optional peripheral travel strokes,
already-played footstep bearing pips, and accepted-ultimate world desaturation
with depth-tested colour retention for visible Toon cast/props/render copies.
All3WorldCueProfile experiment controls default0 pending owner's later choice.
No new HUD framework or edits to completed HUD/tint priority/recede.

Evidence: look-1.5-experiments/report.md. v2 all3cases passed19.393s (actual
sprint/sound guards, real reserved ultimate, body colour and occluding wall).
v3 corrected FPP personal views passed2/2,13.211s. Personally inspected full normal
frames and25percent-grey comparisons plus pip detail crop. Keep first usable look:
speed quiet/peripheral, sound a small hollow8-sector diamond, world grade preserves
cast/can colour. Owner may prefer them off; default decision is external review.
v1 own shader reserved-word error and wrong global speed assumption preserved;
v2 personal shots unhid own head and were INVALID, replaced by v3. Stop iterating.

NEXT: await job98593 and inspect required real release appearance witness (aligned fixture aim with
actual camera sightline, not an off-camera injected aim), then inspect its short
sequence, update TODO/findings and publish1.7. Keep passed lens/charge checks. Read-only
findings: arms already use ToonSkin (lit), no unlit replacement needed. CameraRig
ViewmodelScale=.72,Seat=(0,-.10,.16); root is camera-parented, rotationidentity,
local position gets bob/kick near1296. Carrier.ThrowOriginFor uses AimEye plus
MuzzleForward toward aim point, never viewmodel geometry, so framing must leave
that physics path unchanged. Fixed viewmodel FOV must cover both surface and ink
passes while preserving current world camera75..110 and its aim. Lower/smaller
rest, modest held-slipper rim, anticipation inside the existing charge window;
retain real release/cancel/follow-through. Save a concrete plan before edits.
ThenB,P3,C,D,UX-1,P6,P7. Preserve task IDs and completed HUD/login/mainmenu.
Carry1.2 image finding into1.11/1.12: restoring has a large red grab crescent;
review its meaning/dominance then. Real peer/native/human acceptance remains P7.

Original dirty home-court.png.meta/loading-street.png.meta under Resources/UI/
composition-redesign MUST NEVER be committed. Expected hashes in
Logs/visual1-world-protected-metas.json; original bytes in
Logs/look-1.1-v1/protected-inputs/. Known generated churn (two Inday FppDetails
assets,QualitySettings.asset,TimeManager.asset) restored after1.5v3; protected2
metas verified/restored exactly. Stage by path, never blanket add/reset/clean.
Fetch before push, verify remote HEAD. Sole author M4tyu633, no trailers/AI/em dash.

Unity ONLY through python tools/run_unity_guarded.py, profile
presentation-validation-20260921, never --help. No.cs edits during a run. Before
each run record one question and stopping result here. Smallest changed-behavior
set only; once passed return to implementation. Own defects fixed; unrelated or
environmental failures get at most one retry then exact blocker and independent
work. No intermediate builds/full regression: P7 only, internal Builds/ path.

UX-1 brief and11sketches saved in reports/front-end-flow-2026-09-23 and ArtSource.
Owner correction overrides attached brief: DO NOT TOUCH LOGIN OR MAIN MENU.
New Home opens only from existing TAP TO START. Queued P5.5 before P6/P7. Existing
HUD pass excluded. Required documents were read in order; do not restart intake.

Open carried issues:127.3 v57 chat-line clip recheck and native shutdown0xc0000005
in D3D12Core.dll1.618.1.0 offset0x264831, after all15review stages passed. P7 owns
the native investigation. No clean-exit claim. Preserve original characters,
builders, rigs,GUIDs,UGS IDs and saves. No paid services/resets/delegation/main/
Desktop replacement. Prior ledger is archived with the1.1,1.2 and1.5 reports; it is history,
not the current resume point.

##1.7 concrete 3D framing plan before edits

Use an identity camera-plane projection parent around each procedural ViewmodelArms
instance, including SpectatorCamera's separate POV copy. During only its host
camera render, scale parent X/Y by tan(worldFov/2)/tan(fixed95/2), Zunchanged.
This exactly cancels the world lens for every child surface, ink and attached
effect with no extra camera/RT/duplicate grade and no shader-projection mismatch.
Scope/save/restore the child pose/scale and parent scale around the render so
normal animation, preview and recorded transforms remain unaffected. Existing
CameraRig/Spectator seat constants retain baseline; new WorldCueProfile framing
weight0 restores current size/seat/lens,1 uses .64 scale versus .72 and .08m lower
seat. Apply after their current pose writers, via camera render hooks. Release
the private projection parent and hooks on teardown; preserve imported assets.

Arms already wear lit ToonSkin. Keep world band lighting, remove only private
viewmodel shadow casting to avoid a second world body. Add a small separate
viewmodel-rim shader property for held slipper only, leaving the mirrored world
block/affinity/palette untouched. Zero framing restores0rim. Existing body assets,
materials, animation clips and hand/model builders are not edited.

Inside the existing charge window, add one short wrist preparation pulse at the
start, no input lock or release delay; current HandPreparation/ReleaseHand,
cancel return and follow-through stay owners. ReduceUiMotion suppresses the
extra pulse. Carrier actual ThrowOriginFor is AimEye plus MuzzleForward toward
aim point and does not read viewmodel geometry: leave gameplay trajectory/timing
unchanged. Check apparent held placement and the real physical launch separately.

Small focused batch: same-camera before/after rest and charge frames, held-object
viewport bounds at75/95/110FOV while world lens really changes, launch/aim invariance,
off restoration, lit shader/private shadow/rim properties, real charge/release and
cancel with the anticipation wholly inside existing timing. Use actual main FPP
camera without witness-unhiding its own head; preserve a real channel while
photographing, do not let a frozen frame silently cancel the state. One normal and
comfort/grey25 set, max3looks. Record question/stop before guarded Unity, no.cs edits
in flight, no build or unrelated suite. Then batchB without checkpoint ending.

1.7 first source batch LOCAL/UNVERIFIED: ViewmodelArms.Framing partial with pooled camera scopes, private lens parent, complete nested ownership restoration,95degree apparent lens, .64 rest scale/.08m lower seat, private-only shadow suppression and small held rim through separate Toon property. Existing shader lighting and all materials/palettes retained. SetCharge timestamps one .12s wrist preparation within existing input window; motion reduction suppresses it. New2case Viewmodel fixture checks real world75/95/110 lens against invariant held viewport bounds, unchanged physical launch, both nested camera orders/off restoration/lit material, actual accepted charge/release and restore-protected refusal. Camera screenshots use actual FPP without own-body reveal. Before-run question: does full lens/pose/charge batch compile and retain geometry/ownership/input timing while looking lower/smaller in same-camera normal/comfort/grey25 views? Stop after2fresh cases pass and personally inspected pictures, at most3looks; no native or unrelated tests. No.cs edits during run. Outputs Logs/look-1.7-v1/world.xml,world.log,shots.

1.7 v1 passed both new cases first run. Held-object viewport bounds are invariant
at75/95/110 while the world lens changes; physical ThrowOrigin unchanged; both
nested camera orders/off restore passed; lit Toon/private shadow/rim path passed.
Actual charge wrist pulse, immediate legal release and protected refusal retaining
the item passed. Inspected rest before/after/comfort, allFOVgrey25 and charge frames.
Keep first look: lower/smaller hands leave more centre/right-world space.

One remaining1.7 visual question: actual release from the sightline through its
2-3frame snap/follow-through. The v1 charge fixture injected an aim unrelated to
its camera yaw, so it cannot establish apparent release alignment. Capture a real
aligned release sequence, with actual source projectile/input and FPP rendering,
then judge whether existing ReleaseHand/contact already communicates it or needs
a cosmetic connection. Do not change Carrier's launch rule or invent a bridge
without seeing the sequence. Current SetHolding hides the private shoe immediately
on real release; ThrowGesture.ContactSeconds=.055,FollowSeconds=.18,Release=.54.
No other1.7 tests need repetition unless that new visual question changes code.

1.7 release witness implemented as ONE additional case. It uses real charge/release, actor/camera aligned with injected aim, verifies first physical launch stays at sightline centre, captures34consecutive simulation frames at60Hz plus held frame and pose CSV, then restores Time.captureDeltaTime. No runtime edit since the2/2pass. Question: does the existing .055s snap/.18s follow-through visibly connect to the actual sightline projectile, or is a cosmetic connection needed? Stop after fresh pass and normal/grey sequence inspection. Outputs Logs/look-1.7-v2/world.xml,world.log,shots. No othercases, no build, no.cs writes while running.

look-1.7-v2 stopped at new witness compile: CSV uses FormattableString without its System namespace. Fully qualified it; no runtime change/assertion change. Guardd8b8a6a2beea,job29221 reaped;4churn restored/2metas verified. v3 runs SAME single actual release witness, same question and stop. No1.7base rerun, no build, no.cs edits in flight.

look-1.7-v3 PASSED1/1,10.861s,guard821934e48e00,job50931 reaped;4churn/2metas restored. Physics CSV first flight at(.5007,.5000), correct sightline. Inspected held/first/third full frames and16-framegrey25 sheet. Capture flaw: coroutine renders before LateUpdate, so first image still shows prior held pose despite current physical flight. This is not evidence of a runtime lag. Replaced capture scheduling with a test-only LateUpdate20000 pump after camera/arms, same real input/34frames and20s timeout. No runtime change or assertion weakening. v4 repeats ONLY this release appearance witness; stop after actual post-pose frames inspected. No othertests/build, no.cs edits during run.

look-1.7-v4 PASSED1/1,9.738s,guard79cde9350b60,job67624 reaped;4churn/2metas restored. Post-LateUpdate sequence inspected full first/sixth and16-framegrey25: no runtime held-visibility lag; physical launch remains centred. The actual hand mainly curls in place, so required follow-through needs improvement. Added a small camera-plane right-pivot sweep over EXISTING .055contact/.18follow/.54settle timing, left hand clears slightly, original offsets restored before each pose (no accumulation), disabled by reduced motion/off. Physics untouched. Variant2 release; rest look1 kept. Strengthened same release witness records projected hand tip inside actual render scope and asserts it crosses centre, stays in view and settles to zero. v5 runs ONLY this changed motion/appearance case, stop at pass plus inspected sequence; no other checks/build, no.cs writes in flight.
