# Active TUMP rework ledger

## Current resume: VISUAL-1.5, 2026-09-23

Owner is asleep. Continue every autonomously actionable queued item in THIS
conversation, one agent, without questions or checkpoint endings. Save the exact
state here at each meaningful checkpoint. Do not mark the full goal complete.

DEV: C:/Users/matth/Documents/GitHub/TumbangPreso-Unity-ASTRAReworks, ASTRAReworks.
Current HEAD/verified remote88f9e6e68b0e8b7ba9aac1b03560f8d4ffa1170d published
world1.2; prior479be6a53 published1.1 and f018173c4 UX-1 intake. ACTIVE guarded Unity job66829, look-1.5-v3. Do not edit.cs until completion.
No native/server/browser/helper or build. Original2PNGmetas stay excluded.
Current1.5 source is local and unverified; design/run details below.

Completed1.1: exact ConfinementRadius square at sampled ground heights, rest/armed/
restore sweep; per-camera nearest exit with nested-camera restoration; physical
escape dust/swish excluding teleports/rounds; quiet local air; recorded shared box
and escape playback, personal cues excluded. WorldCueProfile owns off/intensity.
Imported edges are separate read-only meshes named default using Mat_chalk;
material plus whole thin bounds identifies4edges. No mesh/collider mutation.
Third/final look retained: quieter near chalk, stronger far armed chalk, two small
side wisps. DisableBatching fixes the billboard's origin. Do not tune it again.

Evidence: reports/visual-research-2026-09-23/look-1.1-world/report.md.
v8 geometry/all5maps passed1/1 (38.242s); v11 ownership/nested camera/real crossing/
teleport and visual study passed2/2 (15.297s); v3 recorded state and audible-to-muted
replay passed in its2/3 run. Failed case there was the subsequently fixed settled-
movement fixture. Normal and25percent-grey images personally inspected. Controlled
30fps puff study is not free-play or human acceptance. Native/full gate stays P7.
All earlier failures remain in Logs and ledger-history.md beside this report.

Completed1.2: camera-only Defense upper rim plus four open floor brackets for
actual taggable targets; nested views restore original art/identity. Bounded
distance lift for world cast/lata, not environments/viewmodels. Shared close clock
faces viewers, fills blue from actual restore and drains gold from protection.
Existing SyncLata optionally appends version1 and two presentation floats (9bytes),
retaining the old prefix/host guard/protocol50/authority. Existing clip11 State
packs8bit progress with presence bit1024; flags choose restore/protection, values
remain<=2048. No invented clock for legacy clips. Replay reconstructs its own clock.

Evidence: look-1.2-world/report.md beside the1.1 report. v3 wire/legacy/NaN/nonhost/
stale checks passed in3/3. v4 target/nested-camera/catchability passed in2/2. v7
strengthened actual-held restore/cancel/protection/encoding/capture passed1/1,
11.383s. Full and25percent-grey target/clock frames personally inspected. Retain
target look2 and clock look2. No more subjective tuning or passed checks. v5/v6
failures preserved; previous paused photography cancelled the channel, corrected
by freezing Carrier only during photographs, then resuming real input. Cancel now
waits its actual Update/LateUpdate handoff. No weakened assertions. Final static
review also cleans destroyed registry keys and refreshes can renderers on skin
change; next compilation/P7 includes those small lifecycle refinements.

NEXT: implement1.5
leftovers: current MatchFlair.Kind.LataDown and Lata.AnnounceUprightChange already
have NO world text popup (exact literal only in stale comments), so confirm once
and reconcile status, don't invent a deletion. Add V2 meaning/shape/colour/place
header comments to owners, implement/compare peripheral speed, world-only ultimate
desaturation and optional already-audible footstep pips. Owner owns defaults:
keep experimental settings off pending later review, do not ask while asleep.
Then1.7,B,P3,C,D,UX-1,P6,P7 in TODO order. No task deletion or completed HUD redo.
Carry1.2 image finding into1.11/1.12: restoring has a large red grab crescent;
review its meaning/dominance then, without widening1.2. Integrated peer/latency,
stranger interpretation and native replay viewing belong to P7, not claimed here.

Original dirty home-court.png.meta/loading-street.png.meta under Resources/UI/
composition-redesign MUST NEVER be committed. Expected hashes in
Logs/visual1-world-protected-metas.json; original bytes in
Logs/look-1.1-v1/protected-inputs/. Known generated churn (two Inday FppDetails
assets,QualitySettings.asset,TimeManager.asset) restored after1.2v7; protected2
metas verified/restored exactly. Stage by path, never blanket add/reset/clean.
Fetch before push, verify remote HEAD. Sole author M4tyu633, no trailers/AI/em dash.

Unity ONLY through python tools/run_unity_guarded.py, profile
presentation-validation-20260921, never --help. No.cs edits during a run. Before
each run record one question and stopping result here. Smallest changed-behavior
set only; once passed return to implementation. Own defects fixed; unrelated or
environmental failures get at most one retry then exact blocker and independent
work. No intermediate builds/full regression: P7 only, internal Builds/ path.

UX-1 brief and11sketches saved in reports/front-end-flow-2026-09-23 and ArtSource.
Owner correction overrides attached brief: DO NOT TOUCH LOGIN OR MAIN MENU.
New Home opens only from existing TAP TO START. Queued P5.5 before P6/P7. Existing
HUD pass excluded. Required documents were read in order; do not restart intake.

Open carried issues:127.3 v57 chat-line clip recheck and native shutdown0xc0000005
in D3D12Core.dll1.618.1.0 offset0x264831, after all15review stages passed. P7 owns
the native investigation. No clean-exit claim. Preserve original characters,
builders, rigs,GUIDs,UGS IDs and saves. No paid services/resets/delegation/main/
Desktop replacement. Prior ledger is archived with the1.1 and1.2 reports; it is history,
not the current resume point.

##1.5 concrete implementation plan before edits

Source confirms the supposed remaining LATA DOWN world popup is already absent:
MatchFlair.Kind.LataDown calls PlayCanConfirmation only; Lata's shared edge uses
contact burst/accent and voice. Reconcile stale TODO/comments with one focused
event assertion, retaining the existing accepted moment/HUD glyphs.

Experiments extend ColourGrade's existing pass, not a new UI framework. All three
WorldCueProfile controls default0 pending owner's later default decision. They
must be cheap when off and revert exactly to current rendering. Speed lines read
actual local planar motion and sprint/dash intent, keep the centre clear, disable
for reduced effects/motion, panels and blocked gameplay. Sound pips subscribe to
existing AudioDirector.WorldCuePlayed, accept only real step_rubber/deck/swim cues
inside the same32m linear attenuation envelope and above a quiet mix threshold;
ignore own feet, show broad8-sector bearing only and expire quickly. Read pre-user-
volume gain so muted/deaf play works, never query hidden enemy positions or add
proximity warnings. No cues for replay/preview/unowned cameras.

Ultimate desaturation must preserve cast/lata/slippers. Existing environment uses
mixed imported shaders, so do NOT recolour shared materials. Plan: ColourGrade
uses a transient protected-object mask only while the experiment is enabled and
the accepted SharedUltimatePhase is active; draw visible Toon renderers against
the camera's existing depth, desaturate only outside that mask inside the same
grade pass. Include render copies used by UltimateSceneCamera, exclude hidden live
copies. No extra mask allocation/draw at off. Reuse one command buffer/material,
release temporary targets, restore camera state and dispose on teardown. Verify
occlusion and colour retention in the focused comparison; if not clearer, default
stays off and record the result, do not endlessly tune. Personal edge cues disabled
during the shared phase. Completed tint priority/HUD recede remain untouched.

Add short V2 meaning/shape/colour/place comments on actual owner classes, including
new world owners and existing contact/flight/moment/grade owners. No UI art changes.
After batching code, one smallest suite: existing-down-event no duplicate popup,
actual movement/off/comfort cue guards, already-audible step vs far/self/replay,
accepted ultimate preserves object colour/occlusion. Same-camera before/after and
grey25 for each experiment. Max3looks, owner default left as explicit later choice.

1.5 LOCAL complete first batch, uncompiled/unverified: ColourGrade.Experiments partial + ProtectedColourMask resource shader, integrated same-pass edge/world effects, three WorldCueProfile knobs default0, AudioDirector.IsInReplayMix read-only flag, V2 owner comments, and3new WorldCourtCueTests.Experiments cases. Uses explicit mask VP/view uniforms (no camera-matrix global mutation), visible Toon surfaces including render copies, scene-depth occlusion, temporary R8 mask only when on. Real accepted phase fixture pins presentation timestamp for photographs; sprint fixture freezes measured velocity/pose after real travel. Read official Unity DrawRenderer/SetViewProjectionMatrices docs, unlit mask avoids undefined lighting data. No extra UI framework/HUD edits. Before launch question: do controls remain exact-off, real speed/comfort and audible-footstep guards hold, and accepted ultimate grading retain body colour without preserving an occluding wall? Stop after3fresh cases pass and inspected normal/grey25 comparisons. Outputs Logs/look-1.5-v1/world.xml,world.log,shots. No.cs edits during run. No build/full suite.

look-1.5-v1 ended1/3,18.521s,guard53946f6ce801,job69599 reaped. Sound eligibility behavior passed, but all render evidence INVALID: HLSL line is reserved and broke ColourGrade. Renamed to strokeDistance. Sprint case exposed own wrong assumption that base4.6 equals every person's walk speed: actual role/roster-adjusted sprint was3.795. Effect now normalizes the actual role/roster walk scale, reads real Stamina.IsSprinting and read-only PresentationTravelVelocity including slide/lunge impulse. Fixture measures real walk then real sprint, not a global constant; no weakening to a fabricated velocity. Known4churn restored/2metas verified. v2 repeats3new cases for repaired shader/real motion plus corrected captures; same question/stop, no native build.

look-1.5-v2 PASSED3/3,19.393s,guard2fb73910895a,job49152 reaped. Shader repaired; actual sprint/off/comfort, heard cue eligibility, accepted phase, body-colour preservation and wall occlusion passed. Personally inspected normal ultimate +allgrey25 images. Keep ultimate comparison; speed/sound captureINVALID because helper's observedSubject unhid owner's body around FPPcamera. Removed only that witness override for those2FPPcaptures. No runtime changes. v3 runs ONLY those2cases for valid same-camera/grey25 pictures, retaining ultimate pass/images. Stop at pass/inspection, no build. Known4churn restored and protected2metas verified.
