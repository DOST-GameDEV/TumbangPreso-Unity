# VISUAL-1.1 world cues

The court now communicates armed versus safe state on its actual square boundary.
Four imported chalk edges are temporarily replaced at their sampled heights;
throw/home marks, meshes, colliders and rule coordinates are preserved. The local
taggable player sees the nearest exit segment. A real slipper-carrying escape
produces two small dust wisps and a positional swish; offline/network teleports
and round resets are excluded. A quiet filtered air bed follows personal danger.

WorldCueProfile supplies independent intensity/off controls. Replay uses recorded
can flags and sound cues for the shared square and dust, with no private exit/air.
Camera property blocks are saved/restored around nested renders. No gameplay or
network-message changes, completed HUD edits, native build or new UI settings.

## Evidence and limits

- v8-world.xml:1/1,38.242s,guard06285741e63f. All five maps replace exactly4logical
  edges and honor off/rest/armed/restore. maps/ includes20same-camera960x540 frames,
  individual25percent greyscale thumbnails and an inspected contact sheet.
- v11-world.xml:2/2,15.297s,guardce24a39a01fe. Actual local/nested camera, physical
  crossing vs later teleport, and owner/escape appearance checks. local/ contains
  same-camera before/after/comfort at960x540, grey thumbnails and muted escape GIFs.
- v3-world.xml:2/3,62.843s,guardd2ccff328a0a. Retained recorded-state isolation and
  actual audible-precondition to replay-silence pass. Its movement-fixture failure
  was subsequently corrected and superseded by v11, not erased.

All selected normal and25percent-grey evidence was personally inspected. Comfort
frames enable reduced effects, high contrast and HUD120percent. Far chalk look2
is unchanged in final look3; only near width was reduced. Third/final look kept.
The30fps escape GIF samples the actually-triggered effect through its recording
sampler, not free-play timing or external human approval. Stranger interpretation,
human listening and integrated native qualification remain P7.

## Resolved faults, retained history

Native MaterialPropertyBlock allocations moved from MonoBehaviour initializers to
main-thread Build/Play. Offline teleports require their own presentation serial;
MovementEpoch alone covers network movement. Runtime discovery established that
Mat_chalk is a material, nodes are default, meshes read-only and edges separate.
Whole-renderer bounds replace earlier incorrect batching/triangle speculation.
Dynamic batching is disabled for camera-facing puff origin math. Nested cameras
now restore the correct previous exit state. All failed receipts remain in Logs;
the chronological ledger snapshot is ledger-history.md. Do not rerun passed risks.
