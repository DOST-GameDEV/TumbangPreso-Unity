# VISUAL-1.2 targets and shared can clock

The actual taya camera sees Defense-blue upper-body rims and four open brackets
on catchable attackers. Other views keep the authored rim and normal identity
disc; nested renders restore both. Existing reticle/HUD is unchanged. Cast/can
get a bounded distance value lift and wider rim, with no world/environment changes.

The close can clock fills blue during a real restore and drains gold during its
protection. It faces each camera around the actual can centre and depth-tests
normally. It has no collision or gameplay effect. WorldCueProfile supplies off
values. Existing protection-shell presentation is restored at clock-off.

The old SyncLata prefix is unchanged; an optional9byte versioned suffix carries
host-observed progress/protection at10Hz while active and on edges. Legacy prefix,
finite checking, non-host rejection, cancellation and stale progress expiry were
tested in-process. No new message, reset request, authority or protocol bump.
Clip11 records one8bit progress value in the existing bounded can State; old clips
carry no invented timing. Replay has its own clock and no private target overrides.

## Evidence

- v3-world.xml:3/3,24.541s. Retain the wire/legacy/invalid/non-host/stale case.
- v4-world.xml:2/2,16.662s. Retain the taya ownership, nested camera/mesh restoration,
  outside-box exclusion and10m target captures. Target look2 is final.
- v7-world.xml:1/1,11.383s,guard0f87a3f9bb6b. Actual accepted restore remains active
  during photographs, then release cancels it, a new hold completes, protection
  drains, current clock encodes/decodes under the existing bound, legacy state
  gives no clock, and a recorded clock cannot mutate the live can. Clock look2 final.

Normal and25percent-grey target/clock pictures were personally inspected. Comfort
frames use reduced effects, high contrast and HUD120 at960x540. The closest clock
comparison is a controlled freeze of a real accepted hold; it temporarily disables
Carrier updates because a gameplay pause correctly cancels action. It then resumes
real input for cancellation/completion. This is documented staging, not free play.
Real multi-peer latency, mixed-build transport, native replay and stranger/human
acceptance remain P7. No native build or full regression was run.

## Failures kept and interpretations corrected

v1 lacked the existing Netcode assembly reference in the new fixture; v2 lacked
its UI namespace. v3/v4 restore photographs were invalid: pausing cancelled the
hold. Initial attribution to can occlusion was wrong. v5 compared current encoded
protection to a previous-frame render ratio. v6 exposed the same Update/LateUpdate
handoff after real release. v7 explicitly waits the frame and keeps the original
quantization/zero assertions. Failed v5/v6 XML and full chronological ledger are
preserved here; compile logs remain in Logs/look-1.2-v1 and v2. No tests weakened.

Final source review also removes registry keys even if Unity destroyed the can
first and refreshes cached can renderers when the skin changes. Those small
lifecycle edits follow the last focused run and enter the next compilation/P7.
The large existing red grab crescent is queued for1.11/1.12 effects review.
