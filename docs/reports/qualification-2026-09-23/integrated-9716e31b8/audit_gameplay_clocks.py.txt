WalletStore.cs:155  reads DateTime.UtcNow and is not on the list of sites where a wall clock is correct. Either it is a persistent timestamp, in which case add it to PERSISTENT_OK with the reason, or it is a gameplay timer, in which case it is a bug.

27 wall-clock reads across Runtime/ and the core, 33 gameplay-critical files checked, 1 finding(s).
