# Frozen non-UI Windows candidate qualification

Candidate source:23d959912deb3bfe808bd91eb2960a3253c22c82, ASTRAReworks.
This is qualification of the older current-task world/gameplay work, NOT completion
of the shared UI queue or newly added REFINE-2. Remaining original UI/data acceptance
stays explicit. No old task is deleted to make this candidate ready.

Owned detached checkout:
C:/Users/matth/Documents/GitHub/TumbangPreso-Unity-world-qualification-20260923
Source/code is frozen there. The development worktree and concurrent contributors
continue independently. The old validation worktree's111dirty paths stay untouched.
Copy Library as ordinary independent files from the currently stopped development
Editor to keep import cache warm. No hardlinks or shared writable cache directories.

Editor6000.5.8f1, targetWin64. Use only run_unity_guarded.py with the named
presentation-validation-20260921 profile. No help/nographics. Internal output only:
Builds/world-qualification-20260923/TumbangPreso.exe. Determine actual Mono/IL2CPP,
D3DAPI and scene settings from the build manifest/settings, not assumptions.

Validation order, with exact results saved:

1. Record clean checkout identity, packages/settings, known defects and test scope.
   Core and relevant final source audits can run independently of the Editor.
2. Compile/final relevant grouped tests once, including the revised audio-test
   lifetime, current world cues and meaningful backend contracts. Do not silently
   call UI groups qualified; keep their current lane/acceptance boundaries explicit.
3. Build this exact candidate once and inspect real output/identity/errors. Generated
   source churn is recorded, never hidden to claim a clean stamp. Restore only known
   task-generated changes after the run; do not weaken source identity checks.
4. Launch the exact artifact under guarded test profiles. Exercise ordinary/busy/
   low/comfort world play, body/FPP, Rafi/lagoon, spectator/replay and relevant new
   remote lunge start/hold/stop/expiry. Use existing probes where they cover reality.
   Distinguish local scripted views, actual peers and human/device acceptance.
5. Investigate the carried0xc0000005shutdown defect on a bounded reproducible native
   scenario. Record API/driver/log/exit evidence. One hypothesis at a time; no blame
   assigned to D3D12or game code before the comparison supports it.
6. Report exact artifact identity/results/failures and real external limits. Old
   passes are preserved comparisons, never substituted for this candidate result.

Stop each check when its declared question is answered. Tooling repairs remain
bounded; actual product defects get narrow fixes and relevant reruns. No duplicate
full pass for reassurance. Do not replace the Desktop build, erase profiles, run
real paid services or touch another contributor's files/processes. Missing Android
module/handset/physical-pad evidence is a limit, not a passing platform claim.
