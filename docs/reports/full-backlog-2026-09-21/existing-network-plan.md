# Remaining existing-network qualification

Scope:149.4/143.7 current Windows request/rejoin/rehost/outage gaps and155.5 beam
ownership render observation. C4 reservation was released. Retain earlier Mac and
Windows evidence, especially failed-before/passed-after F1/F2 receipts and F3/F4
hold-time/warmup fixes. Do not rebuild a generalized transport layer.

1. Review net_request_safety.py and its opt-in runtime probe against current
   protocol48 before invoking them. Ensure all injected requests use current
   packing/identity and the grader cannot declare a stale/absent scenario passed.
2. Execute current internal Windows candidate through actual host/client duplicate,
   stale-seat, wrong-role and boundary cases, including real rematch, delayed/lossy
   link and killed-client seat reclaim. Preserve exact artifact/runtime hashes,
   profiles, ports, all failed traces and any legitimately skipped tail cases.
3. Cover re-hosted session and bounded link outage using existing net_matrix/net_link
   machinery where it answers the claim; extend only a concrete missing scenario.
   Confirm process cleanup, resource/cooldown idempotence and authoritative refusal,
   not only final transform convergence. Never use an old request schema unchanged.
4. Observe recall-beam ownership from actual peer rendering. A local-owner bool or
   one camera switch is not cross-process rendered evidence. Reuse scoped capture
   instrumentation where possible; keep physical/WAN review separately unverified.
5. Reconcile original open headings to these receipts and existing fixes. Final
   clean-source qualification remains AFTER Rafi/lagoon integration, not an excuse
   to run a full suite repeatedly during feature work.

Only local test processes/services and named guarded profiles; no authentication,
paid relay traffic, real production-account data or another conversation. Existing
native UI/variant work is the current heavy job; this network plan does not launch
concurrent work or replace the rest of the TODO.


## Recall-render implementation prepared while request checks run

New opt-in NetRecallRenderProbe and net_recall_render.py prepare actual host,
owner1, other-player2 and spectator peers. The fourth process first joins a seat
then requests spectator through the normal host API, so its former shoe3 must
also lose private glow. Host stages three loose shoes; ownership flags are never
injected. Every peer must agree on the host epoch/positions and actual mode.
Standardized real-camera same-frame beam-off/on PNGs check that only the proper
owner contributes beam pixels; host/spectator contribute none. Actor/FPP hiding
is scoped/restored solely for the world-only capture. Input/player profiles stay
protected. This is prepared source, not compiled or passed evidence yet.
