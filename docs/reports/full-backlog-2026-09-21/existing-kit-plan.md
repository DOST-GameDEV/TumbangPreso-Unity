# Remaining existing-kit qualification, 2026-09-22

Preserve the implemented six kits, twelve alternatives, authored clips, approved
models/shields, equipment IDs and previous exact receipts. Older design tables are
historical. Equipment-destination.md documents actual nine-point non-dominating
slipper roles, handling/settling, and all24can contact cases; do not redo that work.

1. Reconcile source against the retained per-kit reports and whole-kit decisions.
   F1/F2 prediction receipts and F3/F4 hold/warmup faults have later implementation
   and failed-before/passed-after evidence; final current multiplayer matrix remains.
2. Reproduce potential role inconsistency: Sean E currently has no defender gate,
   although the defender cannot throw. Zack E already refuses this role. Test both
   default/alternative charges; change only if the actual route reproduces waste.
3. Native staged comparison: all24ordinary loadout choices in attacker and defender
   roles, using actual input/cast outcomes, body/FPP/field aftermath and audio.
   Document throw-conversion E as an attacker role trade; test truthful refusal
   and retained resources on defender. Do not invent defender projectiles or call
   staged captures human balance approval. Keep shared cast skeleton for variants
   whose physical verb stays the same; the trajectory/field/timing/sound supply
   the deliberate variant distinction, not a gratuitous differently named gesture.
4. Exercise mixed-kit interruption/counterplay and persistent-field teardown against
   current contracts; inspect missed role/overlap gaps before choosing changes.
5. Qualify final current Windows peer request/rejoin/rehost/outage behavior where
   tooling supports it. Keep unavailable physical-device, WAN and listening checks
   explicit. Then implement Rafi and lagoon; no stopping after this checkpoint.

## Native existing-choice checkpoint

V41completed48unique variant/role cases in caster view and the same48in an actual
other body's camera:44accepted casts and4expected attacker-only E refusals per view.
Correct equipped choice is asserted throughout each capture, not only at setup.
Each role refusal retains the exact charge count and creates no active effect.
This is staged standalone input/camera evidence, not network ownership or freeform
balance proof. Body/FPP/field/audio footage and measured timing are retained.

The v40partial failure remains: the harness incorrectly expected a CannotAct
answer event, while the input buffer deliberately keeps that state silent. The
corrected check observes eligibility plus real press samples and resources.
Initial rush displacement now has its own metric; later shared walking had hidden
the Afterburn travel difference in the old maximum. No mechanical change was made
to those established tradeoffs. Existing dedicated effect/range/window tests remain
the stronger evidence for their precise numbers.

The original Sean native visual route also passed onv41 through real selection and
accepted ultimate, with supported corrected ember geometry. Final native recordings
are being encoded at recorded timing with captured engine audio. Listening and
human taste remain explicitly unverified. Next current peer/request/render checks,
then Rafi/lagoon; this checkpoint does not end the full assignment.
