# UI execution status

2026-09-13. Active UI agent in isolated TumbangPreso-UI checkout, branch ASTRAReworks, base6ef38b02. Parent owns the warm primary Editor and is sole integrator/pusher. No Unity launches or tests in this clone.

## Latest authority
The owner explicitly authorized this one UI subagent to overhaul the ENTIRE UI in parallel with parent map work. This supersedes the older maps-first delay within UI_BRAND_REWORK_PLAN. Do not spawn agents or edit primary. Include all in-game HUD, training, spectator/caster, popups and results. Keep gameplay, persistence and controller-owner contracts. Character maker stays inaccessible, Street Hype stays removed.

## Work underway
- Read current instructions, vision, relevant TODO152 and UI research/plan.
- Reviewed retained Home720p and Play4:3 images. Title is calm/readable. Play has useful grouped decisions but excessive empty bottom area at4:3; body type and focus/Back require systematic review.
- Tracing preparation controls. LobbyChrome currently3314lines with large historical commentary; its actual construction still uses dangling mode tags, tarpaulin, primary burst and different paper treatments. This is the key structural coherence target, not just palette.
- Figma connection confirmed; file I1snFz26WypEywfWMhF9jq. Three-page limit.16existing colour variables,5text styles; no components/screens. Existing variables lack code syntax. Existing logo/illustration image hashes preserved in source JSON.

## Sequence and acceptance
1. Refine common control roles and preparation layout; preserve existing node handlers and Parts contract. Build editable Figma source components and preparation specification. Capture/test actual navigation and all3lobby routes in primary.
2. Character/equipment/hero selection, profile, settings, joins/errors and credits. Investigate immediate picker Back vs PreviewSurface before choosing a fix.
3. Entire in-game UI: Classic/Hero HUD, role/round/time/score, can/slipper/stamina, action/recovery/10s lost-stock prompts, skills/hold-to-read, pause/training, loading/results/rematch and spectator/caster. Coordinate state captures against parent's newer swimming/recovery.
4. Whole task-flow critique at720p/1080p/shortwide/4:3; real hit targets and navigation paths, controller/touch integration, long names, disabled/loading/error states. Focused changed-feature tests only; fresh nonzero NUnit XML. No broad suites or Desktop updates.

## Handoff receipts
No implementation batch committed yet. No new Unity validation claimed. The original assets remain unchanged. Temporary comment-stripped lobby review text is disposable working analysis, not production source.

## Brief audit, latest owner messages
All earlier UI requirements are included:
- OVERALL composition inspiration is the girlfriend's49-page TUMP PDF; pages39-49 are composition ideas, never final layouts. TITLE reference is specifically Slay the Spire's calm restraint: game name, few obvious choices, subtly animated background explaining the can/slipper street game. Do not copy its assets/layout.
- Preserve the girlfriend's original coloured logo/marks unchanged and attributed accurately. Coherent quirky brand palette, warm print character, flexible two-family typography and a natural Filipino hint through street illustration/lettering, without flags or sacred-script decoration.
- Distinguish primary actions, links, tabs, selectors, settings rows, cards, HUD and transient states. Progressive detail, concise English functional copy, proper names, few simultaneous choices, useful Back/focus and live binding glyphs.
- Full scope: home/play; offline/online lobby, join/ready/custom; profile/account/friends/history; all18people/appearance; slippers/cans; hero builds/ability learning; graphics/audio/controls/accessibility and reversible previews; credits; training/tutorial; pause; loading/transitions; confirmations/errors; results/rematch; spectator/caster.
- Full in-game UI: Classic/Hero distinctions, score/time/round/role, can/slipper state, stamina, pickup/throw/reset, cooldown/ultimate readiness, hold-to-read skill information, trip/stun mash/get-up and10second rooftop lost-stock feedback. Parent owns swimming/recovery/authority and later ability changes; coordinate semantic copy rather than inventing mechanics.
- Redesign icons with consistent drawing language and distinguishable meanings/states. Original illustrated background generation is authorized via actual imagegen tool and skill when useful; no guessed model/version, no baked-in controls/text, no replacement/recreation of supplied logo.
- Validate keyboard/mouse, controller and touch; relevant aspects/resolutions/scaling; long names, hitboxes, navigation, disabled/loading/error states. Review actual Unity screens and complete novice/returning task flows, criticize weaknesses and revise. Compilation/Figma alone cannot establish quality.
- Preserve features/source art/saves/profile IDs. Character maker remains inaccessible with implementation/data retained. Street Hype stays removed. Custom Game retained; no redundant STANDARD RULES stepper. No independent controller bridge/MenuNav changes.

## Batch1 receipt
Committed a21b23b35cd0c9b6d3e19c38d4b891f8bd73e1f3: lobby composition and wired-control reskin,2focused BrandPreparationTests. Parent notified for isolated integration validation and captures. No Unity result yet. Early picker-back case deliberately has no capture/delay before hit; a failure is useful retained evidence, not a reason to add blind production delay.
Figma Action component15:11, defaults15:7/focus15:9 and Label#15:0; spacing collection15:2 / variables15:3..6. Fonts read back match Darumadrop One and Work Sans. These represent design work, not final all-screen acceptance.

## Editability and shape direction, newest owner clarification
Keep functional text, shapes, layout and colours easy to edit. Figma uses native editable text/vector/components with named tokens; Unity retains structured UI source and shared theme/control factories. Keep generated art separate and replaceable; never bake text/buttons/icons into backgrounds. Quirky is required: purposeful angled print edges, expressive short headings and original drawn lines from the logo's character, not a generic rectangular dashboard or piles of decorative boxes. PDF layouts are inspiration only; improve them using judgment rather than copying. Current native Figma title18:6/actions and source-control components are editable; raster assets are only retained illustrations/logo.

## Batch2 draft
Visible roster/category selection replaces blind cycling. Existing indexed model preview, Confirm/Dismiss and saved IDs retained. Explicit Use this loadout action; current-versus-preview status. Hero skill board now shows one slot at a time with selectable alternatives, authoritative descriptions/gain/cost/challenge and a separate equip action. Classic has no skill door. Changing categories rebuilds visible choices, preserving focus integration.
NavigationSymbol is presentation-only and now keeps a readable Back/Close label when the existing hit target has room; uses StreetUi factory instead of hiding all text. No ScreenFocus/MenuNav/device ownership code changed. New BrandPickerTests checks cancel/confirm and all hero slots with actual raycast targets and captures. Not compiled/visually accepted yet.

## Preparation validation1 and revision
Parent integrateda21 as537984f6, ran only BrandPreparationTests:1/2. One-frame picker Back passed. Missing-control case failed because test assigned SelectedMode directly, but preparation restores the saved CustomRulesWire; screenshot is truthfully Classic, so Hero-only LoadoutButton is absent. Correct setup now calls the same SetSelectedRules persistence path as PlaySelectionScreen and asserts restored mode; selector errors now identify missing control/active inventory. No gameplay-mode patch and no blind delay.
Visual review of primary Preparation-brand-v3-1920x1080: header is calmer; current screenshot still has old accent font in refreshed profile values and duplicate chevrons. Revised PaperKit's shared short-text font choice to Work Sans and suppress retained Chevron when applying StreetUi. Main action/room art and compact-versus-empty preparation balance still need subsequent visual critique at all3modes. Original failed XML/log remain ui-preparation-agent-v1 in primary.

## Picker validation1 and causal correction
Parent picker-v1:2total,1pass(hero skills),1fail(confirm hit covered by Backdrop). Actual people capture shows ConfirmButton entirely absent, including before reopening. Source trace: PennantEntrance.OnEnable calls ArrowButtonView.AnimateIn even when that component is disabled; AnimateIn sets CanvasGroup.alpha=0 and scale.x=0. The new restyle disabled ArrowButtonView before it could complete entrance, and a subsequent picker reopen replayed its cached entrance. Correction: disable the retired PennantEntrance on the rebuilt picker; StreetUi.Restyle restores the CanvasGroup alpha owned specifically by old ArrowButtonView. No timing delay, no bypass of the real hit test. Existing focused cancel/reopen/confirm test exercises the causal path. Apply the same isolated-entrance handling when rebuilding settings.
Actual hero skill page is readable and returned successfully across3slots; significant empty regions are now intentional progressive detail, but roster still needs more own-brand visual character and richer recognizable thumbnail choices. Keep it open for further visual critique; do not call all UI complete.

## Batch3 draft scope
Settings rebuilt around a stable category column, larger reading rows and existing scroll/input/dropdown contracts. Save/discard/keep-editing is a visible decision instead of an armed invisible-label Back action. Accessibility section adds parent-approved ReducedUiMotion bool(default false) and groups existing slipper highlight there; no graphics runtime fields or persistence format changes. Preference stops title-layer motion, legacy button lift and inspect-panel entry animation; gameplay animations untouched. New focused BrandSettingsTests checks5section composition, preview/cancel/save and actual stopped title phase. Icon source revised for recognizable asymmetric slipper, chunky person, separate friends and ranked symbols, and a proper check mark. Functional text/shapes remain native editable elements.
Picker entrance visibility correction and restrained angled preparation paper edges/tiny retained-caret removal are included. No Unity validation yet for this batch.
Capture caveat: UiRuntimeShots renders overlay canvases through ScreenSpaceCamera/ColourGrade; its PNGs prove layout/hitboxes/state, not exact final UI palette. Do not compensate source colours from them. Final colour acceptance requires a coordinated normal overlay/Game View capture in the primary Editor; no additional UI Editor launched.

# NEWEST OWNER DIRECTION — supersedes the reskin batches above
The owner explicitly says: "make completely new ones" and "explicitly not reuse old ui builders". DO NOT call/wrap PaperKit, PaperCraft, GodotTheme or old screen builders and claim a new screen. DO NOT reparent/restyle old visual hierarchy for convenience. Preserve old source/assets inactive; create genuinely new view builders from empty roots and connect existing gameplay/settings/network/domain behavior through explicit state/event adapters. The prior BrandCharacterSelection/BrandSettingsLayout approach and conservative palette execution are REJECTED, not final. Existing test receipts remain evidence only. Parent was told to avoid another acceptance cycle on reskin batches.

Owner also explicitly rejects text-for-everything: make character choices actual distinct portraits of the18approved models and can/slipper choices actual equipment thumbnails, with short supporting names/tooltips. Use icons for suitable actions/categories/HUD, avoiding unexplained ambiguous symbols.

Owner rejects the lack of a recognizable quirky style and old brown/amber language. Latest two source sheets were directly viewed and copied unchanged:
- original-logo-sheet-owner-latest.png — original Temp codex-clipboard-0d65137f-e808-40dd-9eb0-74c142cffc9b.png; unchanged source artwork.
- palette-sheet-owner-latest.png — original Temp codex-clipboard-a46f3b8f-af1b-4d59-8e4b-73e0e845e44a.png; ten colour swatches + mood references.
Measured source swatches recorded in owner-palette-measured.json: brick A31A24, orange EC6015, apricot F8B366, cream FCD39F, oliveSand918450, yellow F7B722, lime D8D001, hotOrange F35901, olive6D7738, deepOlive42421E. Original logo colours/art remain unchanged. These are authorized UI colours despite stale role-hue decoration restrictions; keep roles clear through labels/icons/context. Use purposeful soft uneven contours, slipper/impact silhouettes, playful proportions and restrained waves/stripes/stamps. Do not produce a cream dashboard of rectangles or merely chamfer one corner. PDF39-49 remain inspiration, not final compositions to copy.

## New architecture now being implemented
- New native TumpUiFactory: empty-root Canvas, editable vector surfaces/icons, proper roles and direct standard uGUI controls. No old visual builders.
- New TumpUiTheme ScriptableObject resource: directly editable colours, type, stroke, spacing and screen sizing; new screen layout data grouped sensibly.
- New TumpHome/Play/Preparation/Pickers/Settings/Overlay/Hud views. Existing SceneFlow/SettingsStore/Roster/network services remain domain authority. Existing Converted controllers may contribute extracted nonvisual state/binding behavior; no old view-generation code is invoked.
- Actual model-derived baked portraits/gear thumbnails generated once through a coordinated parent Editor author window, not runtime34camera cost and not generic avatars.
- Fresh Figma components/screens must show the same broad palette/handmade shape language and real portraits, replacing rejected generic component studies. Preserve IDs/source art. No agents, no extra Editor, no pushes from UI clone.

Future replacement context: the girlfriend will eventually replace this UI with her own, far in the future. This is NOT permission for placeholders or lower quality now. Deliver the full good UI now with replaceable assets, editable theme/layout/shapes/text, and clean view/domain boundaries. Parent authorizes relevant missing skills if materially helpful; installed skills suffice so far. No bulk installation/paid work/additional agents.

Portrait author first result:34actual approved model renders produced successfully in primary and copied unchanged into UI clone. All inspected in two contact sheets; Nemu individually. Readable identities, but too much transparent margin made the people too small for a visual roster. Source diagnosis: ModelPreview zoom is a distance multiplier, so1.55 moved farther away. Revised author to .68 for closer face/outfit portraits and .86 for full equipment silhouettes. Request a second author run in the next coordinated parent window; do not accept undersized thumbnails merely because files exist. Contact sheets retained in reports; no new character art invented.

## First genuinely native batch, source ready
New TumpUiTheme ScriptableObject asset contains exact ten source colours, editable fonts/type/ink/drop/spacing/geometry settings. New TumpSurface draws deterministic editable soft uneven contours, slap/pebble/ticket/portrait/tab/wave roles. New TumpUiFactory builds empty native Canvas/UI objects and standard uGUI controls; no calls to MenuKit/PaperKit/PaperCraft/GodotTheme. NavigationSymbol skips these modern controls so input discovery cannot inject old visual builders.
New TumpPickerView uses real portraits/equipment thumbnails, supporting names, source-colour palette contrast and selected 3D rendering. New TumpSkillView uses new TumpAbilitySymbol editable pictograms, per-slot progressive details and existing HeroBuildRules unlock/equip authority. ConvertedCharacterSelect is a thin adapter: its authored hierarchy is inactive; Wire starts only the native view. Old Wire preserved as an uncalled reference. Only saved pick confirmation, description content and return events are reused as nonvisual behavior. Existing ModelPreview is used exclusively as the approved 3D material/pose renderer, not as a UI screen builder.
New TumpNativePickerTests covers actual portrait assets, no legacy skin components inside native views, cancel/reopen/confirm, all equipment categories and3hero slots. New TumpUiCapture isolates a temporary ungraded UI camera, restores all Canvas/layer state afterward and checks a source cream pixel against the saved palette. This is a targeted capture path, not a substitute for final normal Game View/in-game qualification.
Portraitv2:33accepted photos committed684e9b54; Sike remains a provisional underside/silhouette pending4camera-angle studies fromaf8177df. No blank/generic portrait substitution accepted.
All other new-native screens including home/play/preparation/settings/profile/joins/loading/HUD/training/results/spectator are still required. Previous reskin versions remain superseded and cannot count as their final overhaul. Figma must be updated from rejected rectangular studies to the new palette/forms/portrait system; current earlier control IDs are retained as history, not acceptance.

Sike thumbnail correction: inspected all4camera studies. Reverse reveals the authored upper/white identifying decal; above/below remained flat grey and side obscured it. Chose reverse.png unchanged as final thumbnail; camera-only450pointer-delta yaw in author reproduces it. No model/material/mesh changes. With this correction all34thumbs have been visually inspected and accepted as source assets, pending final in-layout review.

## Latest typography instruction (explicit, not optional)
Owner: "still use darumadrop as main font"; "darumadrop was genuinely good it was just ugly when its really small"; it must stay in SCOREBOARD, SETTINGS and other primary UI, not only headings. Use Darumadrop at generous size with enough spacing; supporting face only for genuinely small/dense secondary details. Native factory now uses Darumadrop for all control/primary labels, roster labels28with taller text bands, traits30, while long descriptions retain Work Sans. TumpUiTheme Inspector documents this primary-face role.
Native picker-v1:2/2 passed; palette pixel exactly252,211,159matches suppliedcream. Actual screenshots reviewed: recognizable portrait-led selection and broadpalette nowread; hero description overlapped state, corrected by allocating more text room and shortening state toEquipped/Preview. Ability symbols were absent and two portrait contours missing incapture; added explicit CanvasRenderer requirement on ability graphic and mesh/cull/alpha/material diagnostics/assertions to focusedcapture. Do not call these visually resolved before fresh evidence. Official CanvasRenderer.GetMesh API verified in Unity documentation for inspection.

Native picker-v2:2/2passed, symbols now visibly render; diagnostic reports nonzero meshes, cullfalse, alpha1and material1 for all symbols/portraits. Actualv2 reveals Back text missing because Darumadrop line height exceeded the40-unit text box, plus long roster names crossing the curved face and selected traits detached under thegrid. Measured TTF metrics: units/em1000, ascent1160,descent-288;28px namesLolaPacing169.12px/MangKanor175.11px, wider than comfortable curved-face reading width. Revision enlarges vertical label space, uses real Back arrow+word, wraps long names inside24pxsideinsets/taller portrait cards, moves traits into selected detail, links Skills beside selected-hero confirmation, and adds an editable printed stage/plinth following actual projectedfeet. No font shrink or model edits. Awaitfreshimagecritique.
Figma continuation blocked by StarterMCP allowance. Nativepalette collection20:2, tenvars20:3..12, nativeDarumadrop/Lime slapcomponent21:2, Label#21:0; existing rejectedstudy15:11 renamed. Need futureFigma portrait uploads/nativeframes onceallowance returns, no paidwork/upgrade/reset attempted. Runtimecontinues.

## Swimming role-marker defect
Parent supplied actual network observerframe Logs/net-swim-classic-v1/observer-frames/00045.jpg: orange role disc floats at submerged capsule feet. Viewed image and traced CharacterNameplate.ApplySizing, which writes capsule-floor localposition once; LateUpdate previously never considers pool surface. Small presentation correction reads RooftopPool.TrySurface while IsSwimming, anchors ring at real surface+existingfloor margin, restores capsule-local placement on exit, and follows actual observer-model headbone for horizontal-swim label position (with surface-height fallback). No colliders, actor transforms, state authority or models changed. Parent reserved CharacterNameplate to UI. Focused SwimmingNameplateTests covers water placement, no UI collider support, and exact land-position restoration. Runtime validation and fresh observer footage pending; do not claim full visual acceptance.

## Native settings/controller/touch batch ready
New TumpSettingsView, TumpFormWidgets, TumpChoice and TumpSettingsSession replace the old settings hierarchy/builders. ConvertedSettingsPanel is now a thin native-entry/back adapter with inactive authored reference. Darumadrop primary rows/controls have generous space; actual input bindings come from Rebinding. Session snapshots GameSettings and binding overrides, restores persisted overrides on discard, and stages reset without erasing saved bindings. Generic-support preference is restored only if user changed it. No RebindSession/GenericPadBridge/MenuNav/device mechanisms edited.
New TumpControllerView uses existing approved PadDiagram art/anchor DATA plus live glyphs/binding APIs; new layouts/components, no old controller screen builder. Native touch controls are built by TouchHud.Native using new surfaces/verb+ability symbols. TouchControls changes are strictly presentation BindNative/Repaint/RefreshIcon/reduced-motion branches; press, release, movement, look, pointer and mapping logic retained. Pure anchor arithmetic remains the saved-offset interpretation contract. New TumpTouchLayoutView edits those real controls, with save/cancel snapshots and a UI-only stick-drag component while the ordinary stick component is suspended. No fake handle size/layout or changes to gameplay input. Unknown future touch entries preserved by resetting only known controls. Existing old TouchLayoutScreen/ControllerMapScreen retained but no longer called by native settings.
FrameRateOptions parent15a76b5a fetched/cherry-picked locally as26d9e2d4; AGENTS header-only conflict resolved by retaining all supplied parent instructions, including sole-parent reset and no further delegation. DO NOT cherry-pick26d9e2d4 back to parent. Added FrameRateLimit default0/Normalize/Apply-after-VSync in GameSettings. NativeGraphicscap uses draftVSync and launchoverride; disabled reason is visible and saved cap retained while sync controls pacing.
Focused TumpNativeSettingsTests has3cases:5native sections+FPSstate/save-discard; controller/touch return+cancel; binding override persistence rollback. New capture handles deliberately overlaid modal palette checks separately and checks visible native symbol geometry. No Unity run or acceptance yet for this batch. Nativepicker-v3 was2/2 and judged substantially improved; leading-icon focus triangle overlap removed in sharedsurface for this nextbatch.
SwimmingNameplateTests parent1/1passed placement/no-collider/land restoration; actual new forward-swim/tread footage remains an in-gameUI review item. All broader native home/play/preparation/profile/joins/credits/loading/HUD/training/pause/results/spectator and icon audit remain OPEN.

PlayerPrefs qualification correction: Rebinding uses shared keytumbangpreso.bindings andTouchLayoutStore uses shared keytumbangpreso.touchlayout; they are NOT profile-namespaced. Parent's named file guard does not restore these. NativeSettingsv1passed3/3 and its normal discard/cancel paths restored semantics, but tests now snapshot exact key existence/string values before each case, restore in teardownfinally and reload action/cache state. Binding mutation also has an immediate try/finally. No production key migration or controller mechanism change. Must retain this protection on any future nativeUI fixture that touches prefs.

## Latest explicit exception: approved controller presentation restored
Owner: "do not fuck with controller look it was good already i wanted it to have those buttons" and "controller to have the lines and shit". This overrides the no-old-builders rule ONLY for the controller map. Restore and preserve the original Runtime/Input/ControllerMapScreen.cs presentation: original PadDiagram artwork, actual callout buttons, connector/leader lines, positions and look. Do not redesign it. TumpControllerView is now only a return adapter that opens this unchanged original screen; rejected native list source is archived in before/TumpControllerView-rejected-native.cs.txt. Settings draft bindings survive original screen entry; the enclosing TumpSettingsSession still owns Save/Discard. No controller mapping/bridge/MenuNav changes. Focused settings test now requires all18callouts, leaders, original diagram and Done return. Corrective batch needs a targeted restored-controller capture, not another controller redesign.

## Native settings v1 visual critique and correction
Parent fixture passed3/3, but actual capture is not accepted: native touch controls collapsed into a lower-left pile because reparented touch Canvas retained its standalone rect instead of stretching to the editing Canvas. Explicitly stretch it before ApplyLayout. Focused test now resets to known defaults within its protected transaction and checks full nested size plus left-stick/right-action separation before/after capture. Slider handles were double-height from vertical stretching plus explicit56sizeDelta; remove the redundant height. Settings binding keys now compact keycaps; nonbinding links are restrained instead of huge full-width filled bars. Selectors have bounded width, scrollable lists show a scrollbar, dynamic text is plain by default. These are source fixes awaiting focused runtime recapture; no visual completion claim.

## Repeated-generator warning and native front-end batch
Owner warns that the previous same-generator approach made every screen similar and ugly. Shared palette/fonts/focus do NOT justify one universal wavy panel/button. Native family audit remains required in actual renders. New title uses a dedicated full-height TumpPaperEdge over separate animated illustration with original logo and four clear choices/credits link. Mode choice uses TumpModeStamp: portrait ensembles on circular print stages with an underlined selection; not another roster card. Play-route actions use recognizable bots/friends/trophy icons and supporting labels. New credits use an open typographic column with unabridged license strings, not dashboard cards. New sign-in uses real native fields with distinct create/sign-in context and disabled busy controls. New home/play/credits build from empty roots; ConvertedMainMenu/ModeSelect preserve old Wire implementations uncalled and route into native views. The settings owner is a new empty object using only its native controller entry, no old hierarchy moved/reskinned. No changes to account/network/domain APIs or saved IDs. Original controller map remains the explicit untouched presentation exception.
New focused TumpNativeFrontEndTests covers title/Play/credits/settings return, actual portraits, Classic versus Hero routes and native sign-in blank-field validation without submitting credentials. Pending coordinated runtime capture; compilation alone is not acceptance. Figma quota remains blocked. All preparation/lobby/profile/joins/loading/HUD/training/pause/results/spectator work remains open.
NativeSettingsv2 parent3/3passed. Actual touch v2 viewed: controls now correctly distributed left/right and slider handles corrected. Still review actual gameplay contrast, real hero signatures rather than fallback stars in layout editor, and sandbox toggle's retained light text before final in-game acceptance. Approved original controller diagram is restored and must stay unchanged.

## Image-generation workflow completed, integration pending capture
Explicit owner request acted on using built-in image_gen__imagegen, whose model version is not exposed. Do not claim GPT Image2.5. Re-viewed original logo/palette sheets and PDF composition ideas. First generated street study rejected as too sepia/fine-detail; targeted style revision simplified into stronger brick/olive/cream shapes. Selected original street-print-v1.png plus separate transparent street-props-v1.png contain no UI text/buttons/logo. Actual PNG alpha0..255 verified. Runtime TumpBackdrop renders plate and two independently positioned/animated can/slipper sprites; theme exposes texture and sprite rectangles, originals and old art retained. Slow14second throw/topple/reset loop respects ReducedUiMotion and application focus; no gameplay authority. Right-side crop is explicit for narrow sign-in illustration area. Prompt history, source paths, role/invariants and critique in ArtSource/ui/owner-brand-2026-09-13/generated-art-manifest.json. Need actual title/sign-in capture with new artwork before accepting composition/motion. NativeFrontEndv1 parent2/2passed on previous art; unchanged functional paths need no broad suite.

NativeFrontEndv1 actual captures reviewed: title has clear primary hierarchy and native Darumadrop, Play mode ensembles show actual distinct models, sign-in fields/context are readable. New generated art replaces the still-detailed old background seen in v1. Credits capture exposes the same scrollbar double-size problem as slider handles: default RectTransform sizeDelta100was added to the stretched track. Explicitly stretch handle before Scrollbar owns it. Add credits bottom reading clearance and focused last-license reachability/text-height assertions. GameVersion label retained quietly at title footer. Do not rerun unchanged screens independently; batch this with new background recapture.

## Imagegen backdrop v2 failure isolated
Preserved primary Logs/ui-native-front-end-v2.xml/.log:2/2failed with Invalid AABB inAABB immediately after MainMenu load, at CanvasRenderer.SetMesh; no suppression. New DrawProps divides source positions by crop width. A first-frame zero-width Canvas produces uv.width0and infinite prop anchors. Guard actual positive rect dimensions before crop/mapping, hide props until layout exists, then resume normally. New targeted PlayMode case exercises zero dimensions and subsequent1920x1080layout with finite-anchor assertions. Also preserve actual PNG dimensions via nPOTScale0: sprite atlas rectangles are expressed against the authored1774x887file, so automatic power-of-two resizing would change their meaning. Pending fresh3case nativefront-end evidence; prior v1passdoes not cover new raster layers.

Backdropv3 still failed3/3with Invalid AABB. The positive-size guard is defensive coverage, NOT an established fix/root cause. Next diagnostic observes each backdrop/prop Graphic via Editor-only BaseMeshEffect before mesh submission, logging first geometry and any non-finite vertex plus rect/anchors/position/scale/pivot/sprite bounds/rect/ppu. Also records imported atlas dimensions and deserialized sprite rectangles. It never changes vertices or suppresses the assertion. Request only the finite-layout case for this diagnostic, then use observed producer to repair before broadening back to actual3front-end cases.

## First native match readout source batch
New TumpMatchReadout replaces Hud.Build with empty-root presentation through Hud.Native. Old HUD source retained uncalled. Clock/4actual portrait score rows/role/can state/stamina/slipper stock/contextual throw-pickup-reset/warmup-vote/recovery-stun/status prompts read current public gameplay state; no gameplay or mapping authority changed. New TumpPowerReadout uses a dedicated circular TumpAbilityDial family, source AbilityGlyph pictograms, live binding glyphs, cooldown/charge/recast/refusal state and one held-reference editorial sheet. It reads current EffectiveName/Description and kit fields, so parent ability changes remain data-driven. New edge effects use existing approved game shaders on new objects and source palette; no new meters. Original YouCard remains dormant when native HUD owns its existing stamina/throw/reset readouts. OffscreenIndicators has new pictured pointers while retained target/projection math remains; spectator toggles/clean feed keep current public Hud API. Native chat construction replaces old builders while preserving the existing chat component's transcript/RPC/AnyTyping/input behavior; no messages sent by tools.
New TumpNativeHudTests2cases stage real Classic/Hero match entry, actual round/role/recovery, power/details and spectator clean feed. Capture helper can now render real arena camera first and native UI separately afterward, preserving overlay colours; this is a composite of actual runtime geometry, not a literal desktop screenshot. Still require live in-game/touch/forward-swim/tread nameplate review. No Unity run yet. RoleSwapCard, pause/results/loading, guided training overlays, preparation/lobby/profile/joins and complete icon/badge audit remain unfinished. Do not claim full in-game UI complete from this initial HUD batch.
Latest icon authority includes ALL ranking/leaderboard/rank badges and other actual icon contexts; inventory existing data instead of inventing progression. Imagegen is a helper, never design authority; off-theme results must be discarded. Parent later audits/improves weaker abilities and preserves polished ones; UI agent must not change mechanics. Original controller diagram/callouts/lines remain preserved exception.
